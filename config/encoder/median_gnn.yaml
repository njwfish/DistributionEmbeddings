_target_: encoder.encoders.DistributionEncoderMedianGNN
in_dim: ${dataset.n_features}
latent_dim: ${experiment.latent_dim}
hidden_dim: ${experiment.hidden_dim}
set_size: ${experiment.set_size}
layers: 2
fc_layers: 2 